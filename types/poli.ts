export type PoliResponse = {
  id: string;
  poli_name: string;
  doctor: string | null;
  poliId?: string;
};

export type ListPoliResponse = {
  
}
export type SelectItem = {
  title: string;
  value: number;
};
